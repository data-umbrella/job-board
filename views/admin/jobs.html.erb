<section class="mt-8 w-5/6 mx-auto">
  <div>
    <p class="text-red-500 font-semibold">
      <a href="/admin/<%= @account.slug %>">
        &larr; Back to dashboard
      </a>
    </p>
  </div>
</section>

<div class="rounded shadow my-4 bg-white w-5/6 p-10 mx-auto">
  <a href="jobs/new" class="float-right">
    <button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline" type="button">
      Add a new job
    </button>
  </a>
  <div class="mb-2 w-4/5">
    <p class="font-bold text-2xl mb-2">
      Job Postings
    </p>
    <div class="markdown-text">
      <p>
         You can add new jobs, edit jobs and delete jobs. <a href="https://www.loom.com/share/13fe92b50bb9496aa501bed41922d5ea" target="_blank">We've created a video tutorial</a> that walks through all the different actions you can take.
      </p>
    </div>
  </div>
  <div class="my-8">
    <table class="table-auto">
      <thead>
        <tr>
          <th class="px-4 py-2">Position</th>
          <th class="px-4 py-2">Company</th>
          <th class="px-4 py-2">Customer</th>
          <th class="px-4 py-2">Date Posted</th>
          <th class="px-4 py-2" span="4"></th>
        </tr>
      </thead>
      <tbody>
        <% @jobs.each_with_index do |job, index| %>
          <tr>
            <td class="border px-4 py-2"><%= job.position %></td>
            <td class="border px-4 py-2"><%= job.company_name %></td>
            <td class="border px-4 py-2"><%= job.owner %><br><%= job.contact %></td>
            <td class="border px-4 py-2"><%= Time.parse(job.date).strftime('%B %d, %Y') %></td>
            <td class="border px-4 py-2 underline">
              <a href="/board/<%= @account.slug %>/jobs/<%= job.slug %>" target="_blank">View</a>
            </td>
            <td class="border px-4 py-2 underline">
              <a href="jobs/<%= job.slug %>/edit">
                Edit
              </a>
            </td>
            <td class="border px-4 py-2 underline">
              <% if job.edit_id %>
                <a href="/board/<%= @account.slug %>/jobs/<%= job.slug %>/<%= job.edit_id %>/edit" target="_blank">
                  Customer Edit Link
                </a>
              <% end %>
            </td>
            <td class="border px-4 py-2 underline">
              <form method="POST" action="jobs/<%= job.slug %>/delete" onsubmit="return confirm('Are you sure you want to delete this job?');">
                <div style="margin:0;padding:0">
                  <input name="_method" value="delete" type="hidden" />
                  <button class="text-red-500 font-semibold underline" type="submit">Delete</button>
                </div>
              </form>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
